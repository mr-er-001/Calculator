#include <iostream>
using namespace std;

int main() {
    cout << "Name: Muhammad Ehsan" << endl;
    cout << "Roll No: 24-SET-033" << endl;
    return 0;
}
using System;

class Program
{
    static void Main()
    {
        // Declare variables
ubtraction
 multiplication
 division
        double number1, number2, result;

        // Get input from user
        Console.Write("Enter first number: ");
        number1 = Convert.ToDouble(Console.ReadLine());

        Console.Write("Enter second number: ");
        number2 = Convert.ToDouble(Console.ReadLine());

        // Check for division by zero
        if (number2 != 0)
        {
            // Divide numbers
            result = number1 / number2;

            // Display result
            Console.WriteLine("The division of {0} by {1} is: {2}", number1, number2, result);
        }
        else
        {
            Console.WriteLine("Cannot divide by zero!");
        }


 subtraction
        int number1, number2, result;
       int number1, number2, sum;
addition

        // Get input from user
        Console.Write("Enter first number: ");
        number1 = Convert.ToInt32(Console.ReadLine());

        Console.Write("Enter second number: ");
        number2 = Convert.ToInt32(Console.ReadLine());

subtraction
 multiplication
        // Multiply numbers
        result = number1 * number2;

        // Display result
        Console.WriteLine("The multiplication of {0} and {1} is: {2}", number1, number2, result);
 multiplication

        // Subtract numbers
        result = number1 - number2;

        // Display result
        Console.WriteLine("The subtraction of {0} and {1} is: {2}", number1, number2, result);
 subtraction
        // Calculate sum
        sum = number1 + number2;

        // Display result
        Console.WriteLine("The sum of {0} and {1} is: {2}", number1, number2, sum);
 addition
    }
}
